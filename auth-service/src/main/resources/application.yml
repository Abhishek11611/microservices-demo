spring:
  application:
    name: auth-service

  profiles:
    active: dev

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1
    validate-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
